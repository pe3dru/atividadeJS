const leia = require("readline-sync");

let n1, n2, i
n1 = leia.questionFloat('Escolhe um numero: ')
n2 = leia.questionFloat('Escolhe um numero maior: ')

if(n1>= n2){
    console.log("Intervalo inválido");
}
for(i=n1 ; i <= n2 ; i++ ) {
    if (i % 3 === 0 && i % 5 === 0 ){
        console.log( i,' e multiplo de 3 e 5 ')
    }

}